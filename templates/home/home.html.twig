{% extends 'base.html.twig' %}

{% block title %}Accueil
{% endblock %}

{% block body %}
	<div class="bg-primary h-[90vh] p-2 shadow-lg">
			<button class="border-2 border-secondary py-1 px-2 text-base cursor-pointer text-secondary transition-transform duration-300 ease-in-out hover:scale-105" onClick="window.history.back();">
				Retour
			</button>
			<div class="flex justify-center">
				<h1 class="text-secondary text-5xl">Accueil</h1>
			</div>
			<div class="flex justify-between">
				<h2 class="p-1 text-secondary">Entrainements en rapport avec votre équipement :</h2>
				<div>
				</div>
			</div>
			<div class="max-md:flex max-md:flex-col lg:grid lg:grid-cols-4 xl:grid-cols-6 gap-4 bg-darkblue mx-auto justify-items-center">
			{% for workout in workouts %}
				<a href="{{ path('show_workout', {'id': workout.id}) }}">
					<div class=" border-secondary shadow-lg bg-primary m-3 w-[12rem] transition-transform duration-300 ease-in-out hover:scale-105" >
						<div class="flex items-center justify-center ">
							<img src="/images/placeholder.png" alt="equipment-image">
						</div>
						<hr>
						<div class="flex items-center justify-center font-poppins text-secondary">
							{{ workout.name }}
						</div>
					</div>
				</a>
			{% endfor %}
			</div>
	</div>
{% endblock %}
